READ ME

Please note that this program MUST be run on a Linux operating system. This is because I have implemented the fcntl() thread locking, which only works on Linux, and compiling the code on any other OS will result in a compile error.
